[package]
name = "tessera_substrate"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "Adapter that wraps the kennguy3n/knowledge substrate (evidence store, observation engine, memory manager, concept graph, synthesis) into Tessera's single-user, single-file model."

[dependencies]
# knowledge substrate crates (git, pinned in the workspace manifest).
knowledge-crypto = { workspace = true }
evidence_store = { workspace = true }
observation_engine = { workspace = true }
memory_manager = { workspace = true }
concept_graph = { workspace = true }
synthesis_pipeline = { workspace = true }

serde = { workspace = true }
serde_json = { workspace = true }
uuid = { workspace = true }
chrono = { workspace = true }
thiserror = { workspace = true }

[dev-dependencies]
tempfile = { workspace = true }

[lints]
workspace = true
